The copy constructor and copy-assignment operator are needed to access StrBlob's shared_ptr and copy or assign the underlying vector (pointed to by the shared_ptr) of the right-hand side object to the left-hand side object. The synthesized versions of these methods will only copy or assign the shared_ptrs, not the vectors beneath. A destructor is not needed because shared_ptrs automatically handle deallocation.