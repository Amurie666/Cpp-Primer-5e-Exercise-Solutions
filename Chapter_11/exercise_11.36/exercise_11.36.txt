The original program will throw an exception. My version will just ignore lines consisting of a key and a single space. It will however register lines with a key and more than one space. This could be a useful mechanism for requesting spaces so that if we have the following rule (ignore the quotes): 6s "      ", it will cause the following translation: Hi 6s bye ==> Hi        end.
