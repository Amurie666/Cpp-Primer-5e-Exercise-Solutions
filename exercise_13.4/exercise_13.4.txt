 Point global;
 Point foo_bar(Point arg)	// Copy constructor possibly called to copy-initialize parameter.
 {
 	Point local = arg, *heap = new Point(global);		// Called to copy-initialize local from arg. Called explicitly to construct the memory to which -
								// - heap points from global.
 	*heap = local;						// Uses assignment operator. Not the copy constructor.
 	Point pa[ 4 ] = { local, *heap };			// Called to copy initialize the first two elements of the array. Rest are default-initialized.
 	return *heap;						// Maybe called to copy-initialize the original call statement from *heap.
 }

