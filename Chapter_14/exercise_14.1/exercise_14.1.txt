Unlike built-in operators, overloaded operators can be called as functions and have the same precedence and associativity as the corresponding built-in operators. Overloaded versions of && and |  | do not preserve order of evaluation nor short-circuit evaluation. 

An overloaded operator has at least one parameter of a class type. Overloaded operators can be used the same way we use built-in operators.